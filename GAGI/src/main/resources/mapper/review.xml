<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="review">
  
  	<resultMap id="ReviewResultMap" type="com.exam.gagi.model.Review">
  		<id property="id" column="ID"/>
        <result property="orderItemId" column="ORDER_ITEM_ID"/>
        <result property="reviewerId" column="REVIEWER_ID"/>
        <result property="targetId" column="TARGET_ID"/>
        <result property="ratingNumber" column="RATING_NUMBER"/>
        <result property="commentText" column="COMMENT_TEXT"/>
        <result property="createdAt" column="CREATED_AT"/>
        <result property="updatedAt" column="UPDATED_AT"/>
  	</resultMap>
  	
  	<!-- 목록 조회 + 검색 + 페이징 -->
  	<select id="selectList" parameterType="map" resultMap="ReviewResultMap">
  		SELECT *
    	FROM (
        	SELECT ROWNUM rnum, t.*
        	FROM (
            	SELECT *
            	FROM REVIEWS
            	WHERE COMMENT_TEXT LIKE '%' || #{search} || '%'
            	ORDER BY ID DESC
        	) t
        	WHERE ROWNUM &lt;= #{endRow}
    	)
    	WHERE rnum &gt;= #{startRow}
  	</select>
  	
  	<!-- 총 게시글 수 조회 -->
  	<select id="selectCount" parameterType="string" resultType="int">
  		SELECT COUNT(*)
        FROM REVIEWS
        WHERE COMMENT_TEXT LIKE '%' || #{search} || '%'
  	</select>
  	
  	<!-- 단일 조회 -->
  	<select id="selectPost"  parameterType="int" resultMap="ReviewResultMap">
  		SELECT *
        FROM REVIEWS
        WHERE ID = #{id}
  	</select>
  	
  	<!-- 공통 CRUD -->
  	<insert id="insert" parameterType="com.exam.gagi.model.Review">
  		INSERT INTO REVIEWS (order_item_id, reviewer_id, target_id, rating_number, comment_text, created_at)
        VALUES (#{orderItemId}, #{reviewerId}, #{targetId}, #{ratingNumber}, #{commentText}, NOW())	
  	</insert>
  	
	<update id="update" parameterType="com.exam.gagi.model.Review">
		UPDATE REVIEWS
        SET rating_number = #{ratingNumber},
            comment_text = #{commentText}
        WHERE id = #{id}
	</update>
	
	<delete id="delete" parameterType="int">
		DELETE FROM REVIEWS WHERE id = #{id}
	</delete>
	
	<!-- 개별 기능 -->
	<select id="getAverageRating" parameterType="int" resultType="double">
		SELECT NVL(AVG(rating_number), 0)
		FROM REVIEWS
		WHERE target_id = #{targetId} 
	</select>
  </mapper>
  